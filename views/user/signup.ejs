<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
  background: linear-gradient(to bottom, #ffcccc79, #ffffff88);
  background-repeat: no-repeat;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0;
  font-family: Arial, sans-serif;
}

.container {
  
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.form-label {
  font-weight: bold;
}

.btn-primary {
  background-color: #ff6699;
  border: none;
}

.btn-primary:hover {
  background-color: #ff3366;
}

.btn-secondary {
  background-color: #6699ff;
  border: none;
}

.btn-secondary:hover {
  background-color: #3366ff;
}

label{
  font-size: 13px;
}

.form-control{
  height: 40px;
}

.container{
  width: 400px;
}

.signup{
  height: 40px;
  font-size: 12px;
}

.google{
  height: 40px;
  font-size: 12px;
}

  </style>
</head>
<body>
  <section class="container mt-5">
    <form action="/signup" method="post">
      <div class="mb-3">
        <label class="form-label">Enter Full Name</label>
        <input type="text" class="form-control" name="name">
      </div>
      <div class="mb-3">
        <label class="form-label">Enter Email</label>
        <input type="email" class="form-control" name="email">
      </div>
      <div class="mb-3">
        <label class="form-label">Enter Phone number</label>
        <input type="text" class="form-control" name="phone">
      </div>
      <div class="mb-3">
        <label class="form-label">Enter Password</label>
        <input type="password" class="form-control" name="password">
      </div>
      <div class="mb-3">
        <label class="form-label">Confirm Password</label>
        <input type="password" class="form-control" name="confirmPassword">
      </div>
      <button type="submit" class="btn btn-primary signup">Signup</button>
    </form>
    <div class="mt-3">
      <form action="">
        <button type="submit" name="googleLogin" class="btn btn-secondary google">Login with Google</button>
      </form>
    </div>
  </section>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
